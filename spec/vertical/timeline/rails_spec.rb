require 'spec_helper'

describe Vertical::Timeline::Rails do
  it 'has a version number' do
    expect(Vertical::Timeline::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
